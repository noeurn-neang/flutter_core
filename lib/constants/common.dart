enum StorageItem {
  user,
  token,
  languages,
  deviceInfo,
  isDarkMode,
  locale,
  expiredDt,
  translationKeys,
  defaultCardIndex,
  defaultCurrency,
}

enum EnvItem { baseApiUrl, authHeadKey }
