## 2.0.0

* Converted from a native plugin to a Flutter package.
* Split into `flutter_core_common` (bootstrap) and `flutter_core` (picker, gallery, countries).
* Removed the project CLI and sample Firebase/signing files.
* Flutter 3.47 / Dart 3.13; Material 3 dialogs, sheets, snacks, and loading.
* `FlutterCoreConfig.colorSchemeSeed` themes the app and overlays.
* `FlutterCore.init(config)` replaces mutating `Variables`.

## 1.0.0

* Initial GetX bootstrap library.
