name: flutter_core
description: Flutter bootstrap core for GetX apps — theme, locale, storage, HTTP, and UI helpers.
version: 2.0.0
homepage: https://github.com/noeurn-neang/flutter_core

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  get_storage: ^2.1.1
  flag: ^7.0.0
  flutter_easyloading: ^3.0.5
  image_picker: ^1.2.0
  crypto: ^3.0.3
  path_provider: ^2.1.4
  path: ^1.9.0
  intl: ^0.20.2
  photo_view: ^0.15.0
  gal: ^2.3.2
  cached_network_image: ^3.4.1
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
