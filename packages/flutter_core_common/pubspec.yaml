name: flutter_core_common
description: GetX bootstrap — theme, locale, storage, HTTP, forms, and avatar display.
version: 2.0.0
publish_to: none
homepage: https://github.com/noeurn-neang/flutter_core

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  get_storage: ^2.1.1
  flag: ^7.0.0
  flutter_easyloading: ^3.0.5
  crypto: ^3.0.3
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
