name: flutter_core
description: flutter_core_common plus camera, gallery, PhotoView, and country select.
version: 2.0.0
publish_to: none
homepage: https://github.com/noeurn-neang/flutter_core

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_core_common:
    path: ../flutter_core_common
  image_picker: ^1.2.0
  photo_view: ^0.15.0
  gal: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
